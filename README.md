# IPC Rust
Un simple projet pour comprendre et experimenter avec l'IPC (Inter Process Communication) en Rust

## Utilisation
Pour lancer l'écouteur :
``` rust
cargo run -p receiver
```
Pour lancer l'émetteur :
``` rust
cargo run -p sender
```
